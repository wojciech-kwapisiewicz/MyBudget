<UserControl x:Class="MyBudget.UI.Main.MainNavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <TextBlock Text="Nawigacja" 
                   Margin="0 0 0 5" 
                   HorizontalAlignment="Center" 
                   Style="{StaticResource HeaderTextBlockStyle}" />
            <Button Content="Start" Command="{Binding NavigateWelcomePage}" />
            <Button Content="Konta" Command="{Binding NavigateAccounts}" />
            <Button Content="Wyciągi" Command="{Binding NavigateStatements}" />
            <Button Content="Operacje" Command="{Binding NavigateOperations}" />
            <Expander Header="Konfiguracja">
                <StackPanel>
                    <Button Content="Reguły" Command="{Binding NavigateRules}" />
                </StackPanel>
            </Expander>
        </StackPanel>
    </Grid>
</UserControl>
