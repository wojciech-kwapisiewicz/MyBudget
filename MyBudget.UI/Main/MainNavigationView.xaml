<UserControl x:Class="MyBudget.UI.Main.MainNavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <TextBlock Margin="0 0 0 5"
                       HorizontalAlignment="Center"
                       Style="{StaticResource HeaderTextBlockStyle}"
                       Text="{lex:Loc MyBudget.UI:Resource:TextBlockNavigation}" />
            <Button Command="{Binding NavigateWelcomePage}"
                    Content="{lex:Loc MyBudget.UI:Resource:ButtonNavigateWelcomePage}" />
            <Button Command="{Binding NavigateAccounts}"
                    Content="{lex:Loc MyBudget.UI:Resource:ButtonNavigateAccounts}" />
            <Button Command="{Binding NavigateStatements}"
                    Content="{lex:Loc MyBudget.UI:Resource:ButtonNavigateStatements}" />
            <Button Command="{Binding NavigateOperations}"
                    Content="{lex:Loc MyBudget.UI:Resource:ButtonNavigateOperations}" />
            <Button Command="{Binding NavigateStatistics}"
                    Content="{lex:Loc MyBudget.UI:Resource:ButtonNavigateStatistics}" />
            <Expander Header="{lex:Loc MyBudget.UI:Resource:ExpanderConfiguration}">
                <StackPanel>
                    <Button Command="{Binding NavigateRules}"
                            Content="{lex:Loc MyBudget.UI:Resource:ButtonNavigateRules}" />
                    <Button Command="{Binding NavigateLanguageSetup}"
                            Content="{lex:Loc MyBudget.UI:Resource:ButtonNavigateLanguageSetup}" />
                </StackPanel>
            </Expander>
        </StackPanel>
    </Grid>
</UserControl>
