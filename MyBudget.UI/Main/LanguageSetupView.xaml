<UserControl x:Class="MyBudget.UI.Main.LanguageSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"  
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Content="{lex:Loc MyBudget.UI:Resource:TextChooseLanguage}" />
        <ComboBox Grid.Column="1"
                  SelectedItem="{Binding ViewModel.CurrentUICulture, Mode=TwoWay}"
                  ItemsSource="{Binding ViewModel.AvailableUICultures}" />
        <Label Content="{lex:Loc MyBudget.UI:Resource:TextChooseInputLanguage}" Grid.Row="1" />
        <ComboBox Grid.Column="1" Grid.Row="1"
                  SelectedItem="{Binding ViewModel.CurrentCulture, Mode=TwoWay}"
                  ItemsSource="{Binding ViewModel.AvailableCultures}" />
    </Grid>
</UserControl>
