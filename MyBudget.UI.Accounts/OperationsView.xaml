<UserControl x:Class="MyBudget.UI.Accounts.OperationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:ownGrid="clr-namespace:MyBudget.UI.Core.Controls;assembly=MyBudget.UI.Core"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"  
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Label Content="Pole " />
            <ComboBox Width="200" 
                      ItemsSource="{Binding ViewModel.ObjectProperties}" 
                      SelectedItem="{Binding ViewModel.FilterProperty, Mode=TwoWay}"/>
            <Label Content=" zawiera " />
            <xctk:WatermarkTextBox Watermark="filtr"
                                   Text="{Binding ViewModel.Filter, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                                   MinWidth="200" 
                                   VerticalContentAlignment="Center"/>
            
            <Label Content="Miesiąc:" 
                   Margin="10 0 0 0"/>
            <xctk:DateTimeUpDown Format="YearMonth" Width="120" Value="{Binding ViewModel.FilterDate, Mode=TwoWay}" />
        </StackPanel>
        <ownGrid:ExtendedDataGrid Grid.Row="1" 
                                  AutoGenerateColumns="False"
                                  ItemsSource="{Binding ViewModel.Data}"
                                  IsReadOnly="True"
                                  GridForType="{Binding ViewModel.GridType}"
                                  LocalizedReflection="{Binding LocalizedReflection, RelativeSource={RelativeSource AncestorType=Window}}"
                  >
            
            <!--<DataGrid.Columns>
                <DataGridTextColumn Header="Description" Width="175" Binding="{Binding Description}" ></DataGridTextColumn>
            </DataGrid.Columns>-->
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Path=Name}" />
                            </StackPanel>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                    <GroupStyle.ContainerStyle>
                        <Style TargetType="{x:Type GroupItem}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type GroupItem}">
                                        <Expander>
                                            <Expander.Header>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Path=Name}" />
                                                    <TextBlock Text="{Binding Path=ItemCount}"/>
                                                    <TextBlock Text="Items"/>
                                                </StackPanel>
                                            </Expander.Header>
                                            <ItemsPresenter />
                                        </Expander>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </GroupStyle.ContainerStyle>
                </GroupStyle>
            </DataGrid.GroupStyle>
        </ownGrid:ExtendedDataGrid>
    </Grid>
</UserControl>
